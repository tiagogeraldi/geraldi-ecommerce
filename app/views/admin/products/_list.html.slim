table.table.table-striped.table-hover
  thead
    tr
      th = @klass.human_attribute_name :id
      th = @klass.human_attribute_name :name
      th = @klass.human_attribute_name :brand
      th = @klass.human_attribute_name :category
      th
      th = @klass.human_attribute_name :price

  tbody
    - resources.each do |item|
      tr
        td.text-right 
          = item.id.to_s
        td 
          = link_to item.to_s, edit_admin_product_path(item)
        td
          = item.brand
        td
          = item.category
        td 
          = product_first_image_for_list(item)
        td.text-right 
          = number_to_currency item.price